// ===============================================
// SEMANTIC + RUNTIME SAFETY TEST
// Intentional error showcase for static and dynamic checks.
// ===============================================

Go live.

// --- BASELINE DECLARATION ---
The account Safety was created.

// --- INTENTIONAL ERROR 1: duplicate account declaration ---
The account Safety was created.

// --- INTENTIONAL ERROR 2: undeclared symbol ---
The account Ghost started with 10 likes.

// --- INTENTIONAL ERROR 3: case-sensitive mismatch (safety != Safety) ---
The account safety started with 1 likes.

// --- SAFE BASELINE VALUE FOR LATER RUNTIME OPS ---
The account Safety started with 10 likes.

// --- INTENTIONAL ERROR 4: division by zero ---
The account Safety now has 10 divide 0 likes.

// --- INTENTIONAL ERROR 5: modulo by zero ---
The account Safety now has 10 modulo 0 comments.

// --- FEED SAFETY CHECKS ---
The feed contains 2 posts.

// --- INTENTIONAL ERROR 6: non-integer feed index ---
The feed at index 1.5 now has 77 likes.

// --- INTENTIONAL ERROR 7: out-of-bounds feed index ---
The feed at index 5 now has 99 likes.

// --- FUNCTION ARITY SAFETY CHECK ---
The story of addone begins with x
{
    Tell back x plus 1.
}
The story ends.

// --- INTENTIONAL ERROR 8: arity mismatch ---
Tell addone with 1, 2.

End live.
